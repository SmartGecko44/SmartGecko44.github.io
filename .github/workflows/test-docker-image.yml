name: Docker Image Creation Test

on: pull_request

permissions:
  contents: read
  packages: write

jobs:
  build_docker_test_image:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Prepare PR title
        id: pr_title
        run: echo "::set-output name=pr_title::$(echo "${{ github.event.pull_request.title }}" | tr ' ' '_')"

      - name: Build Docker test image
        run: |
          docker build -t gxcko.pr:${{ steps.pr_title.outputs.pr_title }}-${{ github.event.pull_request.number }} .

      - name: Save Docker image artifact
        uses: actions/upload-artifact@v2
        with:
          name: docker-image
          path: docker.io/library/gxcko.pr:${{ steps.pr_title.outputs.pr_title }}-${{ github.event.pull_request.number }}
